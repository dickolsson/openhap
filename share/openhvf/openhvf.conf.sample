# Sample OpenHVF global configuration
#
# Copy this file to ~/.openhvfrc
#
# This file contains user-wide settings that apply to all OpenHVF projects

# SSH public key for VM authentication
#
# This key will be installed in the VM's /root/.ssh/authorized_keys during
# initial installation. After installation, all SSH connections use SSH
# agent authentication via SSH_AUTH_SOCK.
#
# You can get your public key with: cat ~/.ssh/id_ed25519.pub
# or: ssh-add -L
#
ssh_pubkey ssh-ed25519 AAAA... user@host

# Optional: shared cache directory for downloaded OpenBSD images
# cache_dir ~/.cache/openhvf

# Optional: default VM name when not specified
# default_vm default

# VM definitions
#
# VMs can be defined in blocks here or in project .openhvfrc files.
# Project config takes precedence over global config.
#
# vm "default" {
#     version      7.8
#     arch         arm64
#     memory       2048
#     disk_size    8G
#     ssh_port     2222
#     console_port 4444
# }
